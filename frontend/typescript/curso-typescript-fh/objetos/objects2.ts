(() => {
  
  let superman: { name: string, age?: number, powers: number[], getName?: () => string } = {
    name: 'Superman',
    age: 40,
    powers: [1, 5, 8],
  }

  let flash: { name: string, age?: number, powers: number[], getName?: () => string } = {
    name: 'Flash',
    age: 28,
    powers: [2, 4, 6, 10],
  }
})();